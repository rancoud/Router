<?php

declare(strict_types=1);

namespace Rancoud\Router;

use Closure;

/**
 * Trait Middleware.
 */
trait Middleware
{
    /*
     * @param $req
     * @param $res
     * @param $next
     *
     * @return mixed
     */
    /*public function next($req, $res, Closure $next)
    {
        $this->before($req, $res);
        $response = $next($req, $res);
        $this->after($req, $res);

        return $response;
    }*/

    /*
     * @param $req
     * @param $res
     */
    /*public function before($req, $res)
    {
    }*/

    /*
     * @param $req
     * @param $res
     */
    /*public function after($req, $res)
    {
    }*/
}
